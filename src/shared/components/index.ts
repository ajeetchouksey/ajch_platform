/**
 * shared/components — canonical import path for shared UI primitives.
 *
 * Usage:  import { Button } from '@/shared/components/ui';
 *         import Layout from '@/shared/components/Layout';
 *
 * Implementation lives in src/components/ during incremental migration.
 */
export * from '@/components/ui';
