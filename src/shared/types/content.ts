/**
 * shared/types — canonical import path for all platform-wide types.
 *
 * Usage:  import type { BlogPostMeta } from '@/shared/types/content';
 *
 * Implementation lives in src/types/ during incremental migration.
 */
export type * from '@/types/content';
