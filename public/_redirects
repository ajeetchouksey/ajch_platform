# Client-side routes (React Router) — always serve the SPA shell.
# (Missing /assets/* files are handled separately by functions/assets/[[path]].js
# — Cloudflare Pages' _redirects only supports 200/3xx status codes, so a
# custom 404 rule here is silently unsupported. See that Function's comments
# for the full story.)
/*  /index.html  200
